import egyik.someFunction

// Context actions megtekintése: Alt + Enter
val a: Int = 5

fun max(a: Int, b: Int): Int {
    if (a > b) {
        return a
    } else {
        return b
    }
}

// Importálás

val person = Person("John", 30)

//Osztály/függvény deklarációjának gyors megtekintése: Ctrl + klikk

fun function() {
    someFunction()
}

//Konzisztens átnevezés: Shift + F6
val b: Int = 10

val maxWidth = b * 0.8

// Multifunkciós kereső előhozása: Shift + Shift
// pl. gradle sync